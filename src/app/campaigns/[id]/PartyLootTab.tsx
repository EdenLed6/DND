"use client";
// Shared party inventory (SPEC-DM §11) — stub, being implemented.
export function PartyLootTab(_props: { campaignId: string; isDM: boolean; myCharacters: { id: string; name: string }[] }) {
  return null;
}
